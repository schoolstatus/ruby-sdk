# bandwidth
#
# This file was automatically generated by APIMATIC v2.0
# ( https://apimatic.io ).

module Bandwidth
  # Domains Model.
  class Domains < BaseModel
    # TODO: Write general description for this method
    # @return [List of Domain]
    attr_accessor :domains

    # A mapping from model property names to API property names.
    def self.names
      @_hash = {} if @_hash.nil?
      @_hash['domains'] = 'domains'
      @_hash
    end

    def initialize(domains = nil)
      @domains = domains
    end

    # Creates an instance of the object from a hash.
    def self.from_hash(hash)
      return nil unless hash

      # Extract variables from the hash.
      # Parameter is an array, so we need to iterate through it
      domains = nil
      unless hash['domains'].nil?
        domains = []
        hash['domains'].each do |structure|
          domains << (Domain.from_hash(structure) if structure)
        end
      end

      # Create object from extracted values.
      Domains.new(domains)
    end

    def self.from_element(root)
      domains = XmlUtilities.from_element_to_array(root, 'Domain', Domain)

      new(domains)
    end

    def to_xml_element(doc, root_name)
      root = doc.create_element(root_name)

      XmlUtilities.add_array_as_subelement(doc, root, 'Domain', domains)

      root
    end
  end
end
