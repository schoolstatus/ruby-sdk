# bandwidth
#
# This file was automatically generated by APIMATIC v2.0
# ( https://apimatic.io ).

module Bandwidth
  # Sites Model.
  class Sites < BaseModel
    # TODO: Write general description for this method
    # @return [List of Site]
    attr_accessor :sites

    # A mapping from model property names to API property names.
    def self.names
      @_hash = {} if @_hash.nil?
      @_hash['sites'] = 'sites'
      @_hash
    end

    def initialize(sites = nil)
      @sites = sites
    end

    # Creates an instance of the object from a hash.
    def self.from_hash(hash)
      return nil unless hash

      # Extract variables from the hash.
      # Parameter is an array, so we need to iterate through it
      sites = nil
      unless hash['sites'].nil?
        sites = []
        hash['sites'].each do |structure|
          sites << (Site.from_hash(structure) if structure)
        end
      end

      # Create object from extracted values.
      Sites.new(sites)
    end

    def self.from_element(root)
      sites = XmlUtilities.from_element_to_array(root, 'Site', Site)

      new(sites)
    end

    def to_xml_element(doc, root_name)
      root = doc.create_element(root_name)

      XmlUtilities.add_array_as_subelement(doc, root, 'Site', sites)

      root
    end
  end
end
